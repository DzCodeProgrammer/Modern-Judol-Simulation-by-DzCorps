# Configuration Files

This directory contains environment and configuration files.

## Structure
- `env/` - Environment-specific configuration files
- `docker/` - Docker configuration files
- `kubernetes/` - Kubernetes manifests